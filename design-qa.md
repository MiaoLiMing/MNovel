# MNovel 设计 QA

## 验收基准

- 参考图：小说聚合 App 第二版视觉稿，覆盖书架、书城、分类、搜索、详情、阅读器、目录、设置、书源和我的页面。
- 目标视口：390 x 844；Widget Test 额外覆盖阅读器视口与左右分页。
- 设计令牌：珊瑚红 `#FF5A3C`、暖白 `#FFF7F2`、浅米 `#F5EFE6`、正文 `#1F1F1F`、分割线 `#E9E4DC`。

## 已验证

- 四栏底部导航显示“书架 / 书城 / 分类 / 我的”，页面切换真实生效。
- 书城频道切换、轮播、快捷分类、精选换一换、更多列表和搜索入口均有真实动作。
- 分类筛选支持单选/组合条件、重置、结果页和返回；搜索支持历史、清空、热词和结果态。
- 详情页支持刷新、来源切换、简介展开、分享、加入/移出书架、目录和开始阅读。
- 阅读器支持控制栏显隐、上一章/下一章、章节边界分页、目录、夜间模式、阅读设置、来源切换和进度保存。
- 目录支持来源切换、正序/倒序、章节跳转、下载当前章节和订阅更新。
- 我的页面支持历史、书源管理、缓存清理、备份恢复、本地导入和设置入口。
- 加载、空态、错误、重试、禁用和成功反馈均有明确视觉状态；未发现空回调或假成功提示。
- `flutter test` 在阅读器视口下通过，`dart analyze` 无问题，Web release 构建成功。

## 风险与限制

- Web 端外部封面受浏览器跨域策略影响时会显示资源回退；移动端使用真实网络图片。
- Android APK 本轮未完成构建，原因是机器 Gradle 无法创建文件锁监听 socket：`No buffer space available`，不是源码编译错误。
- 真实公网书源/媒体线路不在离线测试中伪造成功；相关后端用例采用受控 HTTP mock。

## 结论

- P0：0
- P1：0
- P2：0
- P3：仅保留跨域和构建环境限制

final result: passed
