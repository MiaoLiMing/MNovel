# MNovel 阅读、听书、书源与离线能力完善计划

## 2026-07-30 假数据清零、书源默认启用与规则引擎升级计划（待批准）

### 已确认根因

1. **书架假数据不是卸载残留，而是应用首次启动主动写入**
   - `apps/mobile/lib/data/shelf_store.dart` 在 `shelf.items.v1` 不存在时，把 `curatedCatalog` 前 4 本写入本地。
   - 所以清数据或重新安装后会稳定出现《诡秘之主》《宿命之环》《剑来》《道诡异仙》，这正是截图现象。
2. **最近搜索同样是客户端硬编码**
   - `apps/mobile/lib/features/search/search_page.dart` 在 `search.history.v2` 不存在时直接显示上述 4 个关键词。
   - “热门搜索”来自正式 `/search/meta` 的真实在线响应，不属于本地假历史；本轮保留在线数据，但失败时只显示空状态。
3. **APK 书源被生成器和界面双重默认关闭**
   - `scripts/extract_book_sources.py`、后端完整目录和 Flutter 精简目录均把 1,142 条 APK 源写为 `enabled: false`。
   - `source_management_page.dart` 还禁止 `script_required`、`login_required` 等来源被手动开启；“全部检测”也只检测 `compatible_core`。
4. **当前解析器是项目内自行编写的受限 Python 兼容层**
   - `unified_backend/app/api/v1/mnovel/legacy_runtime.py` 主要依赖 BeautifulSoup CSS Selector 与 `jsonpath-ng`。
   - 当前仅实现旧式 `class.*@tag.*@text`、少量 JSONPath、简单正则替换与 GET/POST；不完整支持 JavaScript、变量状态、Cookie、XPath、分页、登录和 WebView。
   - 因此即使静态审计把 925 条标为 `compatible_core`，也只是“可尝试”，不是已经通过完整搜索、目录和正文验证。
5. **异常还包含真实上游连接失败**
   - 正式目录接口当前可用并返回 1,142 条；抽检基础源仍出现空结果、超时或连接失败。
   - 截图中的 `All connection attempts failed` 是后端访问旧源站失败后透传的 httpx 异常，不是 Flutter 开关本身造成。
   - 这些规则大多来自较早的源站配置；域名失效、TLS/反爬变化和页面改版不能仅靠“全部开启”恢复。
6. **参考 APK 能力边界**
   - `base.apk` SHA-256 为 `8d3194c81f07bd1ecb5faf241a8a0a16e70455535443cde11834c3d867429134`，包名为 `com.readcd.oneiromancy`。
   - APK 含 `assets/bookSource.json`（1,142 条）、`txtChapterRule.json` 和 `javax.script.ScriptEngineFactory` 服务声明，说明其执行链包含完整规则 DSL 与 JVM JavaScript 引擎，而非单纯 CSS/JSONPath。
   - APK 使用 360 加固，DEX 仅能看到壳；本项目不能可靠或合规地复制其实现，只能依据规则格式和可观察行为重建兼容层。

### 实施方案

#### A. 彻底移除生产假数据与兜底内容

1. `ShelfStore` 在键不存在、内容为空或损坏时统一返回空列表，不再写入任何初始书籍。
2. 搜索历史首次进入、空值和损坏数据统一返回空列表，不再注入示例关键词。
3. 删除生产代码中的 `curated_catalog.dart` 及只服务于示例目录的封面资源；测试改用测试目录内的 fixture，不让测试假数据进入生产包。
4. 清理 `ContentRepository` 中仍可触发的本地目录/分类兜底；在线失败时只返回空结果或明确错误状态。
5. 同步清理 `apps/api` 旧开发后端中的示例小说目录，避免切换本地 API 时重新出现同一批假数据。
6. 增加回归测试：全新安装、清除数据、损坏 SharedPreferences、无网络四种场景均不得出现示例书名或示例搜索词。

#### B. 所有书源默认启用，并正确迁移旧状态

1. 提取脚本、后端目录和 Flutter 资产统一生成 `enabled: true`；保留 `original_enabled` 仅作审计信息，不再决定 MNovel 默认开关。
2. 升级启用状态存储版本：新版本首次运行忽略旧资产的默认关闭值；仅用户明确关闭的来源保存覆盖值。
3. 取消界面对 `script_required`、`login_required` 等分类的开关拦截，所有 1,141 个可见来源默认显示为开启。
4. “全部检测”覆盖所有已开启来源，使用有界并发、进度、取消和分批执行，避免同时请求上千站点。
5. 聚合搜索不同时轰击 1,000 多个域名：采用健康度、优先级、失败熔断和轮换批次调度；“开启”表示允许参与调度，不等于每次搜索并发请求全部来源。
6. 将检测结果拆分为“正常、源站不可达、规则不兼容、需要登录、需要 WebView、超时、TLS/HTTP 异常”，不再把所有失败统称为“异常”。
7. 修复空异常信息和 `All connection attempts failed` 的用户提示，保留可诊断的源 ID、阶段和后端错误分类。

#### C. 规则解析器升级（推荐路线）

采用**隔离的 Kotlin/JVM 规则执行 sidecar**，由现有 FastAPI 后端通过内部 API 调用：

1. 使用稳定的通用组件，而不是继续手写 HTML/JS 基础设施：
   - jsoup：HTML5 容错解析、CSS Selector、XPath；
   - Jayway JsonPath：JSONPath；
   - Mozilla Rhino：与参考 APK 时代相近的 JVM JavaScript 执行环境；
   - OkHttp CookieJar：请求、重定向、Cookie、字符集和连接池。
2. 在这些组件之上实现当前 `bookSource.json` 的旧版平铺规则适配器：
   - `searchKey/searchPage`、GET/POST、header、charset；
   - `class/id/tag` 链、CSS、XPath、JSONPath、正则与 `||` 回退；
   - `@text/@textNodes/@html/@href`、索引、排除与替换；
   - `<js>`/`@js:`、`@put:`/`@get:` 状态变量；
   - 搜索、详情、目录、目录分页、正文和正文下一页；
   - 每源 Cookie 会话与必要的登录状态接口。
3. JavaScript 必须运行在独立容器/进程中，禁止 Java 类、文件、环境变量和进程访问，并设置 CPU、内存、执行时间、响应大小、重定向及公网地址限制。
4. WebView/验证码/交互登录规则不伪装为可用：单独标记为“需要交互”，后续通过受控浏览器会话扩展；源站已失效则明确标记“源站失效”。
5. 以 APK 规则资产建立兼容性测试矩阵，分别覆盖 HTML、JSON、GBK、POST、JS、状态变量、Cookie、分页和正文清洗；不以“能访问首页”冒充全链路成功。
6. FastAPI 保留现有公开接口，Flutter 无需理解规则 DSL；上线时新增 sidecar 健康检查、版本号、指标、熔断和回滚开关。

### 第三方项目评估结论

| 方案 | 优点 | 与现有 1,142 条 JSON 的关系 | 结论 |
| --- | --- | --- | --- |
| jsoup + Rhino + Jayway JsonPath | 活跃、通用、可控，可按旧 DSL 精确适配 | 可保留现有 JSON，需实现语法适配层 | **推荐，作为生产 sidecar 基础** |
| LNReader Plugins | MIT、TypeScript 插件体系、测试工具完善 | 每个站点需改写成插件，不能直接读取当前 JSON | 适合未来新插件生态，不适合本轮迁移 |
| Lightnovel Crawler | Python、站点适配器多、带服务端能力 | 站点脚本模型，不兼容当前 JSON；GPL-3.0 | 可参考调度/站点适配，不直接嵌入 |
| Eso（亦搜） | Flutter、多平台，支持 CSS/JSONPath/XPath/JS 等规则 | DSL 不同，需要批量转换；GPL-3.0 | 仅在项目接受 GPL 且愿意换规则生态时考虑 |
| Legado/阅读 3.0 | 规则能力最接近目标格式 | 上游仓库当前只保留侵权风险公告、代码已删除 | **不作为新的稳定依赖**，也不复制参考 APK 代码 |

### 验证与验收

1. Flutter：`flutter analyze`、全量 `flutter test`、Android release 构建。
2. 后端：MNovel 范围 Ruff、全量 Pytest、sidecar 单元/契约/沙箱测试。
3. 全新安装验收：
   - 书架为空；
   - 最近搜索为空；
   - 不出现任何本地示例小说；
   - 书源管理全部默认开启。
4. 规则引擎验收：
   - 从各规则类别抽取固定样本，验证搜索 → 详情 → 目录 → 正文；
   - 输出“解析器失败”和“源站失效”的分离统计；
   - 不承诺已失效域名恢复，但不得再把未执行、连接失败和规则失败混为同一异常。
5. 生产验收：先灰度部署 sidecar 与新后端，再用正式域名验证移动端；保留旧 Python 受限引擎回滚开关。

### 预计改动范围

- `apps/mobile/lib/data/`、`features/search/`、`features/profile/`、相关测试和资源清单；
- `scripts/extract_book_sources.py`、`data/book_sources/`；
- `unified_backend/app/api/v1/mnovel/`、测试与部署配置；
- 新增独立 JVM 规则执行服务及其容器、契约和安全测试；
- 完成后更新 `task.md` 与 `walkthrough.md`。

## 2026-07-29 新版生产后端接口迁移计划（待批准）

### 线上契约核对结果

- 新生产文档与 OpenAPI：
  - `https://api.flowercat.art/docs`：HTTP 200
  - `https://api.flowercat.art/api/v1/openapi.json`：HTTP 200
- MNovel 正式 API 根地址应从 `https://www.flowercat.art/api/v1/mnovel` 迁移到 `https://api.flowercat.art/api/v1/mnovel`。
- OpenAPI 已提供并实测：
  - `GET /health`
  - `GET /home?page=1`
  - `GET /discover`
  - `GET /search`
  - `GET /search/meta`
  - `GET /taxonomy`
  - `GET /content/{content_id}`
  - `GET /content/{content_id}/units?offset=&limit=`
  - `GET /content/{content_id}/chapters/{chapter_index}`
  - `GET /sources/{source_id}/health`
- 线上实测 `xshuquge-authorized` 健康检查为 `healthy`；`b520-authorized` 返回真实结构异常“当前未提供可解析的章节链接”。
- 使用 `xshuquge-139952` 完成详情、前三章目录和第一章正文联调：正文返回 120 段，动态总章节数为 1838。
- 当前 Flutter 主要 DTO 与新 OpenAPI 一致，但有以下偏差：
  1. 默认域名仍是旧的 `www.flowercat.art`。
  2. `/home` 发送了 OpenAPI 未声明的 `channel` 参数。
  3. 章节接口发送了 OpenAPI 未声明的 `source_id` 参数。
  4. 客户端仍静默调用新版 OpenAPI 不存在的 `/favorites/{id}` 与 `/progress/{id}`；这两项本来就是本地状态，应停止无效网络请求。
  5. 通用请求错误只显示 HTTP 状态，未优先解析后端标准 `detail` 信息。

### 实施方案

1. 将 `ApiConfig.baseUrl` 默认值改为 `https://api.flowercat.art/api/v1/mnovel`，继续保留 `MNOVEL_API_URL` 编译参数以支持测试和私有环境覆盖。
2. 严格按 OpenAPI 调整请求：
   - `/home` 只发送 `page`；
   - 章节正文只使用路径参数，不再附加 `source_id`；
   - 书架收藏与阅读进度保持设备本地存储，删除不存在接口的后台同步请求。
3. 抽取统一 JSON 响应检查，优先解析 FastAPI `{"detail": ...}`，保留 UTF-8 字节解码、超时和响应大小保护。
4. 保持上一轮空书城策略：新后端无数据或不可用时展示专业空状态，不恢复本地试读目录。
5. 更新 API 配置、请求 URL、404/detail 和生产 DTO 回归测试；执行 Flutter Analyze/Test，并对正式域名逐接口复验。

### 预期结果

- App 默认直接使用新版 `api.flowercat.art`，书城、检测、详情、目录和正文不再依赖旧域名。
- 请求参数与 OpenAPI 完全一致，不再产生收藏/进度 404 和多余参数。
- 后端错误能够显示真实 `detail`，同时保持网络异常与空状态体验。

## 2026-07-27 书源检测与书城空状态修复计划（客户端已完成，生产部署受阻）

### 已确认根因

1. 生产地址 `https://www.flowercat.art/api/v1/mnovel/health` 返回 HTTP 200，但两个新源的健康检查均返回 HTTP 404“书源不存在”；生产服务器尚未部署上一轮新增的 `xshuquge-authorized` 与 `b520-authorized` 后端代码，因此 App 检测必然显示异常。
2. Flutter `probeSource()` 把后端 404 统一转换成泛化网络错误，没有区分“服务器版本未部署”“上游结构异常”和“请求超时”，用户无法判断异常来源。
3. 书城初始化会先执行 `cachedHome() ?? localHome()`；在线请求失败后，`ContentRepository.home()` 还会再次回退 `_channelFallback()`，所以不可用时仍展示内置试读目录。
4. `HomeData.featured` 当前不可空，仓储层无法表达“请求成功但零本书”或“全部来源不可用”的空结果，页面只能在错误卡片和伪造兜底内容之间选择。

### 实施方案

1. **书源检测**
   - 后端继续以真实上游探测为准：书趣阁返回健康；b520 因章节链接无效返回结构异常。
   - Flutter 保留后端返回的结构化错误详情；404 明确提示“服务器尚未部署此内置源”，超时、连接失败和上游结构异常分别展示。
   - 对检测按钮增加并发代次/挂载保护，避免连续检测时旧响应覆盖新结果。
   - 将包含两个新适配器的 `unified_backend` 部署到生产环境后，再从 App 使用的正式域名验证健康接口。

2. **取消书城本地目录兜底**
   - 书城不再调用 `localHome()`，在线源全部不可用或返回零数据时不读取 `curatedCatalog`。
   - `ContentRepository.home()` 在没有任何在线结果时返回明确空结果/异常，不再调用 `_channelFallback()`。
   - 书城仅展示真实在线聚合结果；本地导入、离线书架仍保留在各自入口，不混入书城。

3. **专业空数据页**
   - 保留顶部频道与搜索入口，主体使用克制的图标容器、标题、说明文字、状态提示和“重新加载”按钮。
   - 区分“没有可用书源”“当前频道暂无内容”“网络请求失败”，但视觉结构一致。
   - 空状态页支持下拉刷新，避免死页面；加载过程设置 8 秒边界，不持续转圈。

4. **验证**
   - 更新书城 Widget 测试：等待期间显示加载态；全部来源不可用后显示空状态；不再出现本地试读书名；点击重试能再次请求。
   - 增加书源检测 404、健康、结构异常和超时测试。
   - 执行 Flutter Analyze/Test、后端 Ruff/Pytest；若生产部署权限可用，验证正式域名两个健康接口与空目录行为。

### 预期结果

- 生产后端部署完成后，书趣阁检测应显示正常；b520 按真实站点状态明确显示“章节链接不可解析”，不再笼统显示网络异常。
- 所有启用书源不可用时，书城显示可刷新、可重试的专业空状态页，不展示任何暂时本地目录。

### 实施结果与生产阻塞

- Flutter 已完成检测错误分类、取消书城本地目录兜底、专业空状态和并发检测防旧响应覆盖。
- GitHub Actions 生产发布已实际触发，但 `production` Environment 未配置 `DEPLOY_HOST`、`DEPLOY_USER` 与 `DEPLOY_SSH_KEY`，在 SSH 步骤以 `missing server host` 失败，未改动线上服务。
- 使用现有专用 SSH 密钥确认服务器可连接，但线上仍是 `/var/www/unified_backend` 的旧式单容器部署；新版部署所需 `/opt/madmin/.env.production` 不存在，Docker 声明引用的 `docker-compose.yml` 也已缺失，当前容器没有可确认的数据挂载。
- 在无法确认数据库持久化和回滚路径前不强制重建容器。正式健康接口因此暂时仍返回 404；新版 App 会明确显示“服务器尚未部署此内置源”，不再笼统显示检测异常。

## 2026-07-27 b520.cc / xshuquge.net 内置源评估与替换计划（已实施）

### 在线检查结论

| 项目 | `https://www.b520.cc` | `http://www.xshuquge.net` |
|---|---|---|
| 首页状态 | HTTP 200，约 27KB | HTTP 200，约 11KB |
| 本次实测首字节 | 约 1.25 秒 | 约 0.41 秒 |
| 页面结构 | 服务端 HTML，书籍路径形如 `/{分片}_{书籍ID}/` | 服务端 HTML，书籍路径形如 `/txt/{书籍ID}/index.html`，章节为数字 HTML |
| robots | 通用抓取 `Disallow: /`，并明确列出多种下载/镜像工具 | `User-agent: *`、空 `Disallow` |
| HTTPS | 可用，Cloudflare | 证书已过期；HTTPS 校验失败，HTTP 可用 |
| 内容声明 | 首页宣传免费提供热门网络小说，详情页称内容由网友提供/转载收集 | 首页宣传免费 VIP 小说，页脚及详情称转载作品、章节由网友上传 |
| 当前判断 | 已按用户确认的授权接入；当前章节链接全部回到首页，因此只提供目录并在健康检查中明确降级 | 已按用户确认的授权接入；HTTP 连接仅发生在后端 |

### 授权与安全边界

1. `robots.txt` 只表达自动访问偏好，不授予小说版权或再分发许可。
2. 两个站点展示大量仍受版权保护的网络小说，并自称转载/网友上传；在未取得站点和相应作品授权前，不批量下载全文，也不把全文作为 MNovel 默认源向用户再分发。
3. `b520.cc` 当前禁止通用抓取。除非站点书面授权并为 MNovel 放行专用 User-Agent，否则不实现自动目录/正文适配器。
4. `xshuquge.net` 只有 HTTP 可稳定访问。即使取得内容授权，也应优先要求其修复 HTTPS；若暂时只能 HTTP，连接只能发生在后端，App 仍只通过 MNovel 的 HTTPS API 访问，并对响应大小、类型和 HTML 结构做严格校验。
5. 用户已于 2026-07-27 明确确认获得授权，且用途为不分发的私人测试；本实现据此启用专用 User-Agent、单源串行限速和短期缓存。

### 推荐数据策略

采用“后端在线解析 + 有界短期缓存”，不采用“整站下载导入服务器”：

- 书城目录、搜索和详情由 `unified_backend` 按需在线获取，TTL 约 10–30 分钟。
- 章节目录按书缓存，TTL 约 30–60 分钟，并保留 ETag/Last-Modified（上游提供时）。
- 正文仅在用户打开或离线下载时按章获取；服务端执行大小限制、正文清洗、章节哈希和短期缓存，不预抓整本、不镜像全站。
- App 的“离线下载”仍是用户主动行为，保存到用户设备；服务端不建立永久全文库。
- 上游失败时使用 last-good 元数据缓存；正文失败明确提示重试/切源，不伪造内容。
- 设置专用 User-Agent、全局并发上限、单域名速率限制、超时、熔断和结构变化告警。

这种方式比整站导入更适合当前场景：内容更新及时、服务器存储和数据库压力小、删除/下架响应更快，也减少持有未经授权全文副本的风险。只有在获得明确的永久托管/再分发授权后，才考虑把授权书库离线导入对象存储和数据库。

### 授权通过后的实施范围

1. **替换现有内置源**
   - Flutter `content_source.dart` 删除 Project Gutenberg、中文维基文库和 Internet Archive 三个默认源。
   - `unified_backend` 的默认 `MNovelService` 移除三个公开来源适配器。
   - 数据库种子和已有内置源迁移同步更新；只删除内置配置，不删除用户自定义来源或本地离线书籍。
2. **新建站点适配器**
   - 为获得授权且允许自动访问的站点实现首页/分类、搜索、详情、目录、正文与健康检查。
   - 使用稳定站点 ID 和前缀，统一输出现有 `ContentSummary`、`UnitSummary` 和 `ChapterContent`。
   - 解析器采用选择器候选、结构断言和夹具测试，避免页面小改动静默返回错误内容。
3. **后端缓存与保护**
   - 复用当前单源超时、last-good 缓存、正文并发合并和章节大小保护。
   - 新增单域名限速、HTTP 上游内容完整性校验、下架名单和缓存主动失效。
   - 不把目标站点 HTML 或全文规则放到 Flutter；App 只调用统一 HTTPS API。
4. **测试与验证**
   - 保存经授权的最小脱敏 HTML 夹具，只覆盖页面结构，不提交大段小说正文。
   - 覆盖目录分页、搜索、书籍详情、章节顺序、正文清洗、404、超时、限流、结构变化和编码。
   - 执行后端全量 Pytest/Ruff、Flutter Analyze/Test、Android Debug APK 构建。

### 当前状态

- 已完成 Flutter 与后端默认源替换；旧内置源退出默认列表，数据库已有旧记录仅停用，不删除历史小说、自定义源或设备离线内容。
- `xshuquge.net` 已实站验证首页、搜索、详情、目录和首章正文；目录会去除顶部倒序“最新章节”造成的错序，实测样本按第一章到第 1837 章排列。
- `b520.cc` 首页目录可用，但详情页章节链接当前全部指向首页；健康检查会返回明确结构错误，阅读端不会无限等待或伪造正文。
- 正文按需在线获取并缓存 30 分钟，没有整站下载或永久导入服务器。

## 2026-07-27 书城慢加载与阅读/听书卡死修复计划（已完成）

### 目标

- 书城优先显示可用缓存或本地兜底内容，不再因某个远端书源变慢而长时间白屏转圈。
- 阅读页不再下载、解析和同步排版整本超长文本，弱网或异常大响应也不能拖死 Flutter UI 主线程。
- 听书复用稳定、有限大小的章节数据，并为系统 TTS 调用增加分段、超时和可恢复错误状态。

### 已验证根因

1. 线上 `GET /api/v1/mnovel/home` 当前一次实测约 `0.75s / 21KB`，但后端冷缓存时会并发等待多个公开书源，现有单源超时为 15 秒，首页没有稳定的“缓存先展示、后台刷新”路径。
2. `unified_backend` 的 `GutendexSource` 和 `InternetArchiveSource` 把整本 TXT 建模为一个“全文”章节。线上 `gutendex-2701` 首章实测约 `1.25MB`，首字节 `5.75s`、完整响应 `9.38s`。
3. Flutter 章节请求固定 8 秒超时，因此大正文会在“下载超时”和“下载成功后继续处理超大 JSON”之间不稳定切换。
4. `ReaderPaginator.paginate()` 在 UI isolate 中对整章反复执行 `substring + TextPainter` 二分测量。把一整本书作为一章会形成长时间同步计算，最终触发 Android ANR。
5. 听书与阅读共用同一正文加载链路；同时 TTS 当前直接朗读原始段落，没有输入长度上限和单次调用超时，所以超长段落或系统语音服务不回调时无法及时恢复。

### 实施范围与方案

#### A. `unified_backend`：把“整本 TXT”转换为有界章节

1. 为 Gutenberg 与 Internet Archive 全文建立统一的正文规范化和分块器：
   - 清理版权头尾和无效空白；
   - 按段落、句末和最大字符数切分；
   - 单章、单段均设置明确上限，禁止产生超大 JSON 字段；
   - 返回稳定的章节序号、标题和实际 `unit_count`。
2. 增加正文/章节缓存与同一图书并发请求合并，避免阅读页、目录和听书重复下载同一本 TXT。
3. `ChapterContent` 返回实际总章节数；首章加载完成后客户端即可获得完整目录边界。
4. 首页聚合增加总时间预算和 last-good 缓存：
   - 单书源失败或超时不阻塞其他来源；
   - 缓存存在时立即复用，刷新失败继续提供旧缓存；
   - 不把正文预下载放进首页同步请求。
5. 增加大文本切章、最大响应尺寸、缓存复用、并发去重、单源超时隔离的后端测试。

#### B. Flutter：缓存优先、动态章节数和超大正文防护

1. 书城首次进入优先展示本地缓存；无缓存时立即展示明确的本地兜底/错误状态，同时后台刷新线上内容。
2. 防止频道快速切换或重复刷新产生旧请求覆盖新请求；页面离开后不再更新状态。
3. 章节请求使用独立且合理的超时策略，并对异常大的旧版服务响应做尺寸/字符数防护，转为可重试错误，禁止送入分页器。
4. `Chapter` 接收服务端实际 `unit_count`；阅读器、目录、进度和听书控制器改用动态章节数，不再把 Gutenberg 永久视为“一章全文”。
5. 分页器只处理有界章节，并优化断句和测量次数；设置变化、窗口变化和重复加载使用 generation/token 丢弃过期结果。
6. 阅读页加载时保留可交互框架；超时、正文过大或书源失败时显示“重试/切换书源”，不无限转圈。
7. 增加书城缓存首屏、过期请求隔离、超大章节拒绝、动态章节数和长文本分页性能回归测试。

#### C. 听书稳定性

1. 将正文拆为 TTS 安全长度的小段，避免把超长字符串直接交给 Android/iOS 语音引擎。
2. 初始化、开始朗读和停止操作增加超时/代次控制；语音服务不回调时进入可恢复错误状态。
3. 切章、暂停、退出和重复点击时取消旧播放循环，避免多个异步循环同时推进段落。
4. 增加超长段落切分、TTS 超时、切章取消和章节加载失败测试。

### 预计修改文件

#### MNovel

- `apps/mobile/lib/domain/content.dart`
- `apps/mobile/lib/data/content_repository.dart`
- `apps/mobile/lib/features/bookstore/bookstore_page.dart`
- `apps/mobile/lib/features/reader/reader_page.dart`
- `apps/mobile/lib/features/reader/reader_pagination.dart`
- `apps/mobile/lib/features/audiobook/audiobook_controller.dart`
- 对应的 Flutter unit/widget tests
- `task.md`
- `walkthrough.md`（保留当前未提交内容，只追加本次记录）

#### unified_backend

- `app/api/v1/mnovel/schemas.py`
- `app/api/v1/mnovel/sources.py`
- `app/api/v1/mnovel/services.py`
- 必要时调整 `app/api/v1/mnovel/router.py`
- `tests/test_mnovel_api.py`

### 验证方法

- Flutter：`flutter analyze`、相关 unit/widget tests、Android debug APK 构建。
- 后端：MNovel 定向 pytest、Ruff/现有静态检查。
- 性能回归：
  - 1MB 以上 Gutenberg/Archive 文本被切成有界章节；
  - 单章响应大小和段落长度不超过约定上限；
  - 首次正文加载不触发 UI 长任务，缓存命中不重复访问上游；
  - 书城有缓存时首屏不等待网络，冷启动失败时也能退出加载态；
  - TTS 超时、暂停和切章后没有遗留播放循环。
- 线上契约烟雾测试：`health`、`home`、`detail`、`units`、`chapters/0`，记录响应时间和响应体大小。

### 批准门槛

用户已回复“1”批准方案；客户端、统一后端、回归测试和 Android Debug APK 构建均已完成。

## 2026-07-25 阅读失败与书源架构修正计划（已完成）

### 已确认根因

1. App 默认请求 `http://114.132.64.216/api/v1/mnovel`。服务器把 HTTP 以 301 重定向到 `https://114.132.64.216/...`，但 TLS 证书签发给域名而不是该 IP，因此 Android 严格校验后报 `CERTIFICATE_VERIFY_FAILED: IP address mismatch`。
2. 翻到下一章时，如果正文不在本地或直连书源中，阅读器会调用上述后端。请求失败后当前实现清空了原章节并展示全屏错误，没有回退到上一章末页，也把底层握手异常直接暴露给用户。
3. 起点、纵横、番茄、七猫、飞卢、晋江和刺猬猫当前只是 `localCatalog` 占位记录，没有登录、授权、目录或正文适配器；“待配置”实际没有可配置内容，属于误导。
4. 三个真正可用的公开内置来源格式并不相同：
   - Gutendex：REST JSON 书目 + Gutenberg 文本文件；
   - 中文维基文库：MediaWiki JSON API + 作品子页；
   - Internet Archive：Advanced Search JSON + DjVu TXT。
5. 上述三个适配器目前主要写在 Flutter `ContentRepository` 中；`MNovel/apps/api` 另有部分重复实现，而 `D:\work\project\AP\unified_backend` 的 MNovel 模块目前只有一个返回两条示例数据的 `/search` 接口。职责重复且生产接口契约不一致。

### 架构决策

- 将“内置公开书源”的查询、格式解析、标准化、缓存、超时和失败隔离统一放入 `unified_backend` 的 MNovel 模块。
- App 只消费统一的书目、目录和正文 DTO，负责 UI、阅读分页、TTS、书架和离线文件。
- 用户自行添加的 JSON/JS 私有书源保留在本机，不上传规则或凭据；自定义书源行可以点击进入真正的编辑配置页。
- 删除七个没有开放适配器的商业平台占位项。它们并非所有内容都收费，但存在登录、会员、版权和反爬约束，当前不应以“待配置书源”展示。
- 不采用忽略证书校验或信任所有证书的做法。生产地址改用证书匹配的 HTTPS 域名；构建时仍支持 `MNOVEL_API_URL` 覆盖。

### 实施范围

1. **阅读器容错**
   - 跨章前保留当前章节和末页快照；
   - 下一章加载失败时回滚到原章节末页；
   - 将证书、超时、断网、404 和书源正文缺失转换成可读错误；
   - 提供“重试”和“切换书源”，不再出现不可恢复的空白阅读页。

2. **App 书源管理**
   - 内置列表只保留 Gutendex、中文维基文库、Internet Archive；
   - 删除商业站点及“自定义示例”占位项，并清理旧启停覆盖；
   - 分开显示“内置公共书源”和“我的书源”；
   - 自定义来源整行可进入编辑；内置来源点击显示格式、能力、数据策略和健康状态；
   - 健康检查改为调用统一后端的书源探测接口。

3. **统一后端 MNovel 模块**
   - 从现有 MNovel API 迁移并整理 `home/discover/taxonomy/detail/units/chapter/source-health` 契约；
   - 建立统一 `NovelSourceAdapter` 接口，为三种异构公开来源分别实现适配器；
   - 加入并发聚合、去重、超时、缓存与单源失败隔离；
   - 删除当前示例搜索结果；
   - 增加契约测试和来源适配器 Mock 测试。

4. **生产地址**
   - App 默认 API 地址从裸 IP 改为 `https://www.flowercat.art/api/v1/mnovel`；
   - 后端未部署完整 MNovel 路由前保留本地/公开来源降级，避免再次阻塞阅读；
   - 不修改 Android TLS 安全策略来绕过证书。

### 验证方法

- Flutter：分页跨章失败回滚、友好错误、自定义书源编辑、内置来源详情 Widget 测试。
- unified_backend：MNovel 路由契约、三类适配器解析、超时和部分来源失败测试。
- 静态检查：Flutter Analyze、Ruff。
- 构建：Web Release、Android Debug/Release APK。
- 网络烟雾：验证域名 TLS、`source-health`、首页、目录与章节接口。

### 批准记录

用户已回复“1”，批准同时修改 `MNovel` 和 `D:\work\project\AP\unified_backend` 两个工程。

## 状态

- 当前状态：已实施并通过自动化测试与构建验证。
- 实施原则：保留工作区现有未提交改动，尤其不覆盖 `apps/mobile/pubspec.lock` 当前镜像地址变更。
- 产品边界：只接入公开、授权或用户自行配置的来源；不伪造书源可用状态，不把静态书目包装成真实在线书源。

## 一、现状诊断

### 1. 阅读器翻页

- `ReaderPage` 的 `PageView.builder` 以“章节数”为 `itemCount`，每一页实际是一整章。
- 阅读区左右点击、滑动、自动翻页都直接调用章节切换，所以不存在章内页码。
- 阅读进度只保存 `chapterIndex` 和按章节计算的比例，无法恢复到章内具体页面。

### 2. 翻页动画

- 设置中虽有“覆盖 / 仿真 / 滑动 / 无”四种枚举，但渲染端除了“无”禁用滑动外，其他模式统一使用同一个 `PageView + PageScrollPhysics`。
- 当前没有按模式应用位移、透视旋转、阴影或即时切换，因此三种动画看起来完全一致。

### 3. 听书

- 当前没有 TTS 服务、播放状态、语音/语速/音调设置、章节队列、定时关闭和听书页面。
- 阅读页也没有固定的听书入口。

### 4. 书源与书城

- Flutter 内置目录只有 8 本书，FastAPI 的 `CATALOG` 也是同一组静态书目。
- 书城首页请求失败后固定回退到这 8 本，因此内容长期不变。
- 内置的起点、纵横、番茄、七猫等条目类型均为 `localCatalog`，只有名称和网址，没有搜索、详情、目录、正文适配规则；它们不是可工作的在线书源。
- 自定义 JSON / JS 来源目前主要支持一次性返回书目列表，详情、目录、正文规则和分页能力不完整。
- 后端已有 Project Gutenberg 在线目录，但默认每次最多 10 条，首页仍以静态目录为主，且中文内容覆盖有限。

### 5. 下载与离线书库

- “缓存当前章节”只是向 `SharedPreferences` 写入单章 JSON。
- 没有小说级下载任务、下载索引、章节完成度、失败重试、空间统计或删除能力。
- 详情页、书架和书城卡片没有读取离线状态。
- “我的”中的下载列表是示例数据，不对应真实缓存内容。
- 当前缓存管理的分类和删除键也与实际 `offline.chapter.*` 数据不一致。

## 二、实施方案

### 阶段 A：建立真实章内分页模型

1. 新增阅读分页器，根据实际可用视口、字体、字号、行高、字距、段距、首行缩进和横竖屏设置，使用 `TextPainter` 计算分页。
2. 引入明确的阅读位置模型：
   - `chapterIndex`
   - `pageIndex`
   - `pageCount`
   - `characterOffset`
3. 阅读器维护当前章节页面，并预加载相邻章节：
   - 当前章未读完时只切换页码；
   - 当前章最后一页继续向后时，进入下一章第一页；
   - 当前章第一页向前时，进入上一章最后一页；
   - 章节加载失败时停留当前页并提供重试，不跳过章节。
4. 字号、行距、屏幕旋转或主题设置改变后重新分页，通过字符偏移尽量保持原阅读位置。
5. 阅读进度升级为版本化结构，兼容已有仅含章节进度的数据。
6. 底部状态显示“本章页码 / 本章总页数”和全书章节进度；目录与章节滑杆仍按章节跳转。
7. 自动翻页改为逐页推进，到章末自动加载下一章。

### 阶段 B：实现四种可辨识翻页效果

统一由同一个分页状态机驱动，动画层只负责呈现，避免模式切换破坏阅读位置。

- 滑动：页面跟手水平平移，释放后按速度和距离决定回弹或翻页。
- 覆盖：新页保持在底层，当前页向外覆盖式移出，并显示克制的边缘阴影。
- 仿真：加入透视矩阵、绕 Y 轴旋转、页面明暗和折页边缘，形成清晰可辨识的纸张翻动效果。
- 无：点击后即时切页，不播放过渡动画；禁用横向拖拽，避免误认为动画未生效。

补充边界：

- 动画期间锁定重复提交，避免跳两页。
- 低性能设备控制动画时长与阴影开销。
- 系统“减少动态效果”开启时自动降级为“无”或短滑动。

### 阶段 C：加入专业听书功能

1. 使用系统 TTS 能力，Flutter 侧接入当前稳定的 `flutter_tts`，Android / iOS 优先使用设备语音，避免依赖付费云服务和上传正文。
2. 新增单例 `AudiobookController`，负责：
   - 播放、暂停、继续、停止；
   - 上一章、下一章与自动连播；
   - 当前段落和章节进度；
   - 语速、音调、音量、语音角色；
   - 15 / 30 / 60 分钟及“本章结束”定时关闭；
   - 中断恢复、页面退出后的状态保持和异常提示。
3. 阅读页右下角增加固定悬浮听书按钮：
   - 避开底部工具栏和系统安全区；
   - 控件显示播放/暂停状态；
   - 点击进入独立听书页。
4. 新增行业常见的听书页面：
   - 封面、书名、作者和当前章节；
   - 可拖动章节进度；
   - 中央播放键、前后章节、快退/快进一段；
   - 语速、定时、声音、目录入口；
   - 当前朗读段落摘要与加载/不可用状态；
   - 深浅色和横竖屏适配。
5. 朗读完成一章后预取下一章并继续，失败时给出重试和切换书源入口。
6. Android 增加 TTS service query；iOS 配置合适的音频会话。无中文语音包时明确引导用户安装/选择系统语音，不静默失败。

### 阶段 D：把“名义书源”改造成可工作的聚合体系

1. 扩展统一书源契约，至少覆盖：
   - 首页发现与分页；
   - 搜索；
   - 详情；
   - 章节目录；
   - 正文；
   - 可用性检测、超时、重试和来源优先级。
2. 为 JSON / JS 自定义来源补齐分阶段规则执行，不再只支持“一次性书目数组”。
3. 增加书源配置导入能力：
   - URL 导入；
   - 剪贴板 / 本地 JSON 导入；
   - 批量启停、去重和导入预览；
   - 格式校验及危险 URL 拦截；
   - 对常用阅读源格式做可维护的兼容子集转换，无法支持的规则明确列出原因。
4. 内置来源按真实能力标识：
   - 有完整适配器的来源才参与聚合；
   - 只有网址、没有规则的来源标记为“需要配置”，不再显示为可用；
   - 保留公开版权目录作为开箱即用来源，并扩展分页、分类和搜索。
5. 重构书城聚合：
   - 并发查询启用且健康的来源；
   - 设置单源超时和最大并发；
   - 按书名 + 作者归一化去重并合并可切换来源；
   - 分页加载、下拉刷新、随机化/时效排序；
   - 本地短期缓存用于弱网首屏，但后台刷新后内容会变化；
   - 展示数据来自哪些来源、最后更新时间及部分来源失败提示。
6. 书城首页不再依赖固定 8 本：
   - 在线来源成功时构建动态轮播、精选、热门和最近更新；
   - 在线来源全部失败时展示“缓存内容 + 明确的离线提示”；
   - 首次无缓存且无可用来源时引导用户管理/导入书源，不用静态假数据掩盖错误。
7. 搜索采用多源聚合与逐源状态，允许用户查看命中来源、切换来源和重试失败来源。

### 阶段 E：建立可靠的离线下载中心

1. 新增独立的离线仓储，不再把大量正文直接堆进 `SharedPreferences`：
   - 正文按小说 / 来源 / 章节写入应用数据目录；
   - 元数据保存小说快照、已下载章节、总章节数、时间、大小和状态；
   - Web 测试环境保留轻量存储降级实现。
2. 提供下载范围：
   - 当前章；
   - 后续 20 / 50 章；
   - 全本；
   - 仅未下载章节。
3. 下载队列支持有限并发、取消、失败重试、断点续下和来源失败后的显式切换。
4. 读取章节时优先使用匹配来源的本地正文，离线也可进入阅读和听书。
5. 增加真实“下载管理 / 离线书库”页面：
   - 全部、下载中、已完成、失败；
   - 小说级进度、已下载章数、占用空间、最近下载时间；
   - 继续下载、重试、查看、删除。
6. 入口与标识：
   - “我的”增加“离线书库”入口和数量；
   - 书架卡片、书城卡片和详情页展示已下载 / 部分下载标识；
   - 详情页下载按钮根据状态显示“下载”“下载中”“已下载”；
   - 章节目录逐章显示离线状态。
7. 修正缓存管理统计和清理逻辑，删除前确认，删除后同步刷新所有标识。
8. 对旧 `offline.chapter.*` 数据执行一次性迁移，确保已有缓存不丢失。

## 三、预计代码范围

### Flutter

- `apps/mobile/lib/features/reader/reader_page.dart`
- `apps/mobile/lib/features/reader/reader_settings.dart`
- 新增阅读分页、翻页动画、听书控制器和听书页面
- 新增离线模型、离线仓储、下载队列和下载管理页面
- `apps/mobile/lib/data/content_repository.dart`
- `apps/mobile/lib/data/source_store.dart`
- `apps/mobile/lib/domain/content_source.dart`
- `apps/mobile/lib/features/bookstore/*`
- `apps/mobile/lib/features/detail/content_detail_page.dart`
- `apps/mobile/lib/features/shelf/shelf_page.dart`
- `apps/mobile/lib/features/profile/*`
- `apps/mobile/pubspec.yaml`
- Android / iOS 平台配置

### FastAPI

- 扩展来源适配接口、动态目录聚合、分页和缓存
- 调整首页 / 发现 / 搜索响应中的来源状态和刷新信息
- 补充来源并发、去重、失败隔离和公开目录测试

### 项目文档

- 更新 `task.md`
- 更新 `walkthrough.md`
- 必要时补充书源格式、离线数据和隐私边界文档

## 四、验证方案

### 自动化

- Dart Analyze 无错误。
- Flutter Widget / Unit Test：
  - 长章节能分成多页；
  - 页内切换不改变章节；
  - 章末进入下一章第一页，向前行为对称；
  - 设置变化后阅读位置稳定；
  - 四种翻页模式走不同渲染路径；
  - TTS 状态机、自动连播、定时停止和错误恢复；
  - 离线迁移、下载队列、状态聚合和离线读取；
  - 书城动态聚合、去重、分页和部分失败。
- FastAPI Pytest / Ruff：
  - 动态来源聚合；
  - 单源超时与失败隔离；
  - 去重和来源合并；
  - 分页与缓存刷新。
- Flutter Web release 构建，确保条件实现不破坏现有 Web 验证。

### 人工验收

- 360 × 800、390 × 844、430 × 932、768 × 1024。
- Android 真机重点验证 TTS、返回键、系统语音缺失、前后台切换和横屏。
- iOS 构建条件允许时验证音频会话、静音键/耳机中断和安全区。
- 弱网、断网、来源超时、章节缺失、下载中断、存储不足。

## 五、完成标准

- 左右翻页先翻章内页，读完本章才进入下一章第一页。
- 四种翻页设置具有明确不同且稳定的视觉行为。
- 阅读页右下角可进入完整听书页，支持可用的系统 TTS 播放控制、章节连播和设置。
- 书城由真实启用来源动态聚合，不再永久固定为 8 本；来源不可用时信息透明。
- 所有实际下载的小说都能在离线书库查看，并在书架、书城、详情和目录显示一致状态。
- 原有书架、阅读进度、自定义来源和旧章节缓存得到兼容或迁移。

## 2026-07-29 三站统一规则源与三栏导航实施计划（已批准）

### 目标

- 将 `shuchong.info`、`xntk.org`、MIUI 阅读器入口统一为同一套后端规则协议。
- Flutter 只消费统一 DTO，不在客户端硬编码网站解析逻辑。
- 底部导航固定为“书架 / 书城 / 我的”，分类与榜单合并到书城顶部。
- 上游不可用时显示真实空状态或明确异常，不回退到本地试读目录，也不无限加载。

### 方案

1. 后端建立 JSON 规则模型，统一搜索/发现、详情、目录、正文和全链路健康检查。
2. HTML 使用 BeautifulSoup CSS Selector；JSON 使用 `jsonpath-ng`；保留字符集、正则清洗、允许域名和响应上限。
3. 为三个站点分别提供规则文件，但共用一个执行器、HTTP 客户端和错误模型。
4. Flutter 切换到 `https://api.flowercat.art/api/v1/mnovel`，移除旧参数和不存在的收藏/进度写接口。
5. 书城采用“推荐 / 分类 / 榜单”信息架构，书源管理只展示三项并准确标注未检测、正常和异常。
6. 发布前执行后端全量测试、Flutter Analyze/Test/Web/APK 构建；发布后验证 MIUI 的发现→详情→目录→正文链路。

### 现实边界

- MIUI 当前公开 JSON 接口可完成完整链路。
- `shuchong.info` 当前域名返回停放/跳转脚本，`xntk.org` 当前返回 HTTP 503；规则保留，但健康检查必须如实返回异常，不能伪造小说数据。

## 2026-07-30 书架演示数据清理计划（待批准）

### 根因

- `ShelfStore._read()` 在 `shelf.items.v1` 不存在时，把 `curatedCatalog` 前四本写入书架。
- 这些条目的书名、章节数、阅读进度、未读角标和更新时间均为演示数据，不来自生产 API。
- 演示条目一旦写入 SharedPreferences，升级和刷新仍会继续显示。

### 实施方案

1. 新安装或没有书架记录时返回并持久化空列表，不再注入 `curatedCatalog`。
2. 读取已有 `shelf.items.v1` 时，仅删除已知演示 ID；保留用户从真实书城加入、导入或下载的书籍。
3. 同步清理被删除演示 ID 对应的 `reading.progress.v1`，不影响其他书籍阅读进度。
4. 空书架继续使用现有专业空状态，并引导用户前往真实书城。
5. 增加回归测试，覆盖首次启动为空、旧演示数据迁移、真实书籍保留和迁移幂等性。
6. 执行 Flutter Analyze/Test，重建 Web 和 Android Release APK，并在浏览器中验证书架不再出现演示书。

## 2026-07-30 APK 书源提取与兼容集成计划（待批准）

### 一、目标

- 从 `D:\work\project\AP\base.apk` 的 `assets/bookSource.json` 提取书源配置，建立可追溯、可去重、可检测的内置书源目录。
- 将可兼容的小说书源接入 MNovel 的搜索、详情、目录和正文完整链路，而不是只在“我的 → 书源管理”中展示不可执行的网址。
- 对暂时不兼容、已失效、需要登录或属于音频/特殊类型的规则如实标记，不伪造“正常”状态，也不让它们拖慢聚合搜索。
- 截图仅作为 APK 提取失败或个别记录缺失时的补充依据；已确认截图中的站点除 `bixiaobook.com` 外均能在 APK 内置规则中命中。

### 二、只读分析结论

- APK 是有效 ZIP/APK，内置 `assets/bookSource.json`，大小约 1.94 MB，共 1,142 条记录。
- 文件采用旧版阅读/Legado 风格的扁平规则字段，包括：
  - `bookSourceName`、`bookSourceUrl`、`bookSourceGroup`、`bookSourceType`；
  - `ruleSearchUrl`、`ruleSearchList`、搜索结果字段；
  - 详情字段、`ruleChapterList`、章节名/章节地址；
  - `ruleBookContent`、下一页规则、字符集、请求参数和 User-Agent；
  - 少量 `$ref` 引用、内嵌 JavaScript、JSONPath、正则替换和特殊变量。
- 截图中的 MIUI、567 中文、书虫中文网、爱豆看书、酷我听书、有兔文学、读一读、笔趣阁、无弹窗、燃文、天涯、猎鹰、免费读书、630 看书、名著阅读、神笔屋、无图、幽冰、爱奇文学、小说酒吧等均有对应规则。
- 当前 `SourceStore.importMany()` 检测到 `bookSourceUrl` 后会主动拒绝导入，提示“请先转换规则”。
- 当前 MNovel 自定义 JSON/JS 书源仅支持读取已经符合 MNovel `ContentItem` 结构的书目清单；尚不能解释 APK 中的搜索、详情、目录和正文规则 DSL。
- 因此不能通过“批量写入 1,142 个网址”达到真实集成；必须先增加规则模型、兼容执行器、健康检测和并发隔离。

### 三、范围与安全边界

- 仅解析用户提供 APK 中的静态书源配置，不绕过账号、付费、DRM、验证码、签名校验或访问控制。
- 不移植 APK 的广告 SDK、加固库、应用代码或其他无关资源。
- 默认仅启用无需登录、无需私有令牌且完整链路检测通过的普通小说源。
- 需要登录、Cookie、验证码、付费权限、音频专用或包含高风险脚本的规则仅导入元数据并保持关闭。
- 规则只能访问书源自身声明并经过校验的 HTTP(S) 主机；限制重定向、响应体大小、超时、并发和脚本能力。
- 书源只提供技术索引能力；用户应仅访问其有权阅读的内容，应用不内置或复制第三方小说正文。

### 四、实施方案

#### 阶段 A：可复现提取与清单审计

1. 新增只读取 APK ZIP 资源的提取脚本，输出原始 SHA-256、提取时间、记录总数和字段统计。
2. 将 `bookSource.json` 转换为 MNovel 的版本化规则清单，保留原始字段，解析 `$ref`，统一布尔值、类型、分组、优先级和稳定 ID。
3. 对域名和规则去重；同域多规则保留为独立变体，不以域名粗暴覆盖。
4. 生成兼容性报告，区分：
   - 可直接兼容；
   - 需要补充 DSL 能力；
   - 需要登录/验证码；
   - 音频或非小说；
   - 地址非法/规则缺失；
   - 重复引用。
5. 将 `bixiaobook.com` 作为截图补充候选，仅在能取得完整合法规则时加入；只有名称和网址时标记“待配置”，不伪装成可用源。

#### 阶段 B：后端规则模型与受限执行器

1. 在 FastAPI 侧新增明确类型的 Legacy 书源模型，覆盖请求模板、字符集、搜索、详情、目录、正文和分页规则。
2. 先实现高覆盖、可测试的兼容子集：
   - GET/表单 POST、相对地址、`searchKey`/`searchPage` 等模板变量；
   - UTF-8/GBK 解码；
   - CSS/旧版 `tag`、`class`、`id` 选择表达式；
   - JSONPath；
   - `@text`、`@html`、属性读取、索引、排除、简单正则替换；
   - 搜索 → 详情 → 目录 → 正文的 URL 解析和下一页。
3. 不直接执行任意 Java/Android API 脚本；内嵌 JavaScript 进入受限兼容层，禁止文件、进程和任意网络访问。无法安全转换的脚本规则标记“不兼容”。
4. 每个请求执行域名白名单、DNS/重定向复核、内网地址拦截、超时、响应大小上限、并发上限和失败熔断。
5. 通过统一后端 DTO 向 Flutter 返回书源状态和小说数据，避免在移动端复制一套复杂解析器。

#### 阶段 C：批量导入、检测与启用策略

1. 导入全部可解析配置到书源目录，但首次默认关闭未经检测的第三方源。
2. 增加批量健康检测队列，至少验证：
   - 站点可达；
   - 搜索能返回结构化结果；
   - 任取一个结果可打开详情；
   - 目录非空；
   - 任取一章可获得有效正文。
3. 只有完整链路通过的普通小说源才标记“正常”并允许参与聚合；部分链路通过时显示准确失败阶段。
4. 同一域名批量失败时进行熔断；书城/搜索只并发请求用户启用且最近健康的有限数量书源，避免 1,142 源同时请求。
5. 保存检测时间、耗时、失败原因和兼容等级，支持重新检测和按状态批量启停。

#### 阶段 D：Flutter“我的 → 书源管理”

1. 将 1,142 级别列表改为懒加载/虚拟列表，增加搜索、分组和状态筛选。
2. 显示总数、已启用、正常、异常、待检测、待兼容和需登录数量。
3. 书源详情展示来源域名、原分组、规则能力、检测阶段、最后检测时间和失败原因。
4. 提供“检测所选”“仅启用检测通过”“停用全部异常”等批量操作。
5. 保留用户自定义书源，稳定 ID 去重，不覆盖用户已有启停和排序设置。

#### 阶段 E：截图缺失源补充

1. APK 规则兼容和检测完成后，再处理截图中缺失的 `https://www.bixiaobook.com`。
2. 若只能确认网址而无法得到搜索/详情/目录/正文规则，则作为“待配置”记录加入，不参与聚合。
3. 若站点已失效、跳转到无关内容或需要绕过访问控制，则不接入，并在报告中说明原因。

### 五、预计代码范围

- APK 提取/转换/审计脚本及生成的版本化规则资产。
- `apps/api/app` 下的书源模型、受限 HTTP 客户端、规则解析器、健康检测和接口。
- `apps/api/tests` 下的规则夹具、安全边界、解析链路和故障隔离测试。
- `apps/mobile/lib/domain/content_source.dart`
- `apps/mobile/lib/data/source_store.dart`
- `apps/mobile/lib/data/content_repository.dart`
- `apps/mobile/lib/features/profile/source_management_page.dart`
- 必要的 Flutter 测试、项目文档、`task.md` 和 `walkthrough.md`。

### 六、验证方案

- 提取一致性：APK 哈希固定时，转换结果稳定；1,142 条原始记录均有明确去向，不能静默丢失。
- 转换单测：覆盖 `$ref`、重复域名、GBK、POST、相对 URL、JSONPath、HTML 选择器、正则、分页和不兼容脚本。
- 安全测试：拒绝内网/本机地址、跨域恶意重定向、超大响应、无限分页和越权脚本。
- 后端测试：执行 Pytest、Ruff，并对代表性兼容源做搜索 → 详情 → 目录 → 正文链路测试。
- Flutter 测试：执行 Analyze、Unit/Widget Test，覆盖千级列表、筛选、批量启停、状态展示和旧数据迁移。
- 构建验证：Flutter Web release 与 Android release APK。
- 人工验收：在“我的 → 书源管理”确认总数、分组、搜索、状态和批量检测；在书城/搜索确认只聚合已启用且健康的源。

### 七、完成标准

- APK 的全部 1,142 条记录都被解析、去重并归类，形成可复现审计报告。
- 兼容且检测通过的源可完成搜索、详情、目录和正文全链路；失败源显示真实原因。
- 未兼容规则不会被错误标记为可用，也不会拖垮书城和搜索。
- 截图中的书源均有“已集成、待兼容、待配置或已失效”的明确结论。
- 原有三个书源、用户自定义书源和现有书架/阅读数据得到兼容迁移。

### 八、实施结果（2026-07-30）

- 已完成 1,142 条 APK 规则的提取、审计、兼容分级和移动端目录集成。
- 已将三个主规则源固定置顶，用户自定义源排列在 APK 千级目录之前。
- 已将千级可拖拽列表替换为惰性普通列表，避免无意义的拖拽状态与持续帧调度。
- 已修复静态 Future 缓存复用挂起任务导致目录永久加载的问题，仅缓存成功解析的数据。
- Flutter 全量 38 项测试、Analyze、Web Release 与 Android Release 构建均通过。
