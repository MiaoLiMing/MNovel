# MNovel 设计 QA

## 验收范围

- 参考图：用户提供的推荐、分类、榜单、我的、书源管理 5 张移动端截图。
- 实现视口：390 × 844。
- 生产数据：`https://api.flowercat.art/api/v1/mnovel`。
- 核心流程：书城真实内容 → 小说详情 → 正文阅读 → 听书页面；我的 → 书源管理 → 全部检测。

## 同屏对照结论

- 书城保留参考图的“推荐 / 分类 / 榜单”、男女切换、搜索、轮播、快捷入口和分区列表结构。
- 底部导航按用户最新要求固定为“书架 / 书城 / 我的”，不再保留单独分类入口。
- 我的页面采用轻量卡片、数据摘要与设置入口，信息层级与参考图一致。
- 书源管理仅显示三个内置规则源；首次进入均为“未检测”，执行“全部检测”后显示真实延迟和正常/异常状态。
- 视觉继续使用 MNovel 既有暖白与珊瑚红设计令牌，没有照搬参考应用的品牌图标和文案。

## 生产态验收

| 页面/状态 | 结果 | 证据 |
|---|---|---|
| 三栏导航 | 通过 | `qa/screenshots/bookstore-production.png`、`profile-production.png` |
| 书城真实数据 | 通过 | 推荐页显示 MIUI 返回的《修仙狂徒》等书籍 |
| 书源初始状态 | 通过 | `sources-undetected.png` 中三项均为“未检测” |
| 书源真实检测 | 通过 | `sources-detected.png`：MIUI 正常，另外两项按上游现状异常 |
| 小说详情 | 通过 | `detail-production.png` |
| 正文阅读 | 通过 | `reader-production.png`，显示真实章节正文与 4637 章总数 |
| 听书页面 | 通过 | `tts-production.png`，正文段落已进入播放队列 |
| 控制台错误 | 通过 | 应用内浏览器 error/warn 日志为空 |

## 可接受差异

- 参考图来自另一款产品，封面、品牌名、底部图标和书籍内容不复制；MNovel 使用真实在线返回内容。
- Web 端系统 TTS 能力取决于浏览器语音实现；Android APK 使用系统 TTS，已有超时、分段和播放代次保护。
- `shuchong.info` 当前返回停放/跳转页面，`xntk.org` 当前不可用；界面如实显示异常，不伪造成功数据。

## 结论

- P0：0
- P1：0
- P2：0
- P3：仅保留上游域名可用性和 `flutter_tts` 未来 Kotlin/Wasm 兼容性提示

final result: passed
